extends layout
include mixins/crearProyecto

block content 
    div.contenido-principal
        h1 #{nombrePagina}

        if errores
            each error in errores
                div.alerta=error.texto
        +crearProyecto(proyecto)

         